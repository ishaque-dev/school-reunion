name: school_reunion
description: School Reunion Alumni Directory (Clean Architecture + BLoC)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  bloc: ^8.1.2
  equatable: ^2.0.5
  get_it: ^7.6.4
  dartz: ^0.10.1
  http: ^1.1.0
  google_fonts: ^6.1.0
  url_launcher: ^6.2.2
  shimmer: ^3.0.0
  image_picker: ^1.0.7
  image: ^4.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  bloc_test: ^9.1.5

flutter:
  uses-material-design: true
